<%= bootstrap_form_for NewsletterSubscription.new do |form| %>
  <%= form.text_field :email, id: :newsletter_email, class: 'form-control-lg', placeholder: 'email', hide_label: true %>
  <%= form.submit t('newsletter_subscription.new'), class: 'btn btn-lg btn-primary' %>
<% end %>