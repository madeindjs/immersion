<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item" aria-current="page"><%= link_to 'Prestations', categories_path %></li>
    <li class="breadcrumb-item" aria-current="page"><%= link_to @product.category.name, @product.category %></li>
    <li class="breadcrumb-item active" aria-current="page"><%= @product.name %></li>
  </ol>
</nav>
<h1><%= @product.name %> <small>proposé par <%= link_to @product.user.complete_name, @product.user %>  dans <%= link_to @product.category.name, @product.category %></small></h1>
<p><%= @product.description %></p>
<%= link_to 'Editer', edit_product_path(@product), class: 'btn btn-warning' if @product.user == current_user %>